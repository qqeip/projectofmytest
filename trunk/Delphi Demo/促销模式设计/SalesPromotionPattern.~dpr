program SalesPromotionPattern;

uses
  Forms,
  UnitMain in 'UnitMain.pas' {FormMain},
  UnitSimpleFactoryPattern in 'UnitSimpleFactoryPattern.pas',
  UnitStrategyPattern in 'UnitStrategyPattern.pas',
  UnitCommon in 'UnitCommon.pas',
  UnitFactoryUnionStrategyPattern in 'UnitFactoryUnionStrategyPattern.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TFormMain, FormMain);
  Application.Run;
end.
